<div class="row mt-4">
  <div class="col-2">
    <%= link_to "Back", todos_path, class:'btn btn-secondary' %>
  </div>
<div>
<div class="row set-margin justify-content-center">
  <div class="col-10">
    <div class="card text-center">
      <div class="card-header <%= @todo.complete ? 'text-success' : 'text-danger' %> " id="font-size-card">
        <%=  @todo.complete ? "done" : "It's not done"%>
      </div>
      <div class="card-body">
        <h5 class="card-title">Task : </h5>
        <p class="card-text"><%= @todo.task %></p>
        <%= link_to 'Edit', edit_todo_path(@todo), class: 'btn btn-outline-primary' %>
        <%= link_to 'Delete', todo_path(@todo), data:{turbo_method: :delete, turbo_confirm: 'Are you sure?'},
                                              class:"btn btn-danger " %></td> %>
      </div>
      <div class="card-footer text-muted">
      <small>Created at <%= time_ago_in_words(@todo.created_at) %> ago ,
       Updated at <%= time_ago_in_words(@todo.updated_at) %> </small>
      </div>
    </div>
  </div>
</div>







